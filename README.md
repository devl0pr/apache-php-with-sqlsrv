# apache-php-with-sqlsrv

if you get ssl error just add this variables `ENCRYPT=false;trustServerCertificate=true` to db connection.

```sh
"sqlsrv:Server={SERVER};Database={DB};ENCRYPT=false;trustServerCertificate=true"
 ````
